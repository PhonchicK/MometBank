<Window x:Class="MometBank.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MometBank" Height="700" Width="900" 
        Background="#F0F2F5"
        WindowStartupLocation="CenterScreen" FontFamily="Segoe UI" >

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                SnapsToDevicePixels="True" >
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#B0BEC5"/>
                                <Setter Property="Foreground" Value="#78909C"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Model Card Style -->
        <Style TargetType="Border" x:Key="ModelCardStyle">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#888" BlurRadius="8" ShadowDepth="2" Opacity="0.25"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollViewer style to hide horizontal scroll -->
        <Style TargetType="ScrollViewer" x:Key="NoHorizontalScroll">
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="Auto"/>
            <!-- Pagination -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
            <TextBox Width="300" Height="36" 
                     Margin="0,0,10,0" FontSize="14"
                     VerticalContentAlignment="Center"
                     Text="{Binding TagSearchText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Filtrele" Style="{StaticResource ModernButton}" Command="{Binding FilterCommand}"/>
            <Button Content="Yeni Ekle" Style="{StaticResource ModernButton}" Command="{Binding NewCommand}"/>
        </StackPanel>

        <!-- Models List -->
        <ScrollViewer Grid.Row="1" Style="{StaticResource NoHorizontalScroll}">
            <ItemsControl ItemsSource="{Binding PagedModels}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ModelCardStyle}" Width="180" Height="240">
                            <StackPanel>
                                <Image Width="160" Height="160" Stretch="Uniform"
                                       Margin="0,0,0,10"
                                       Source="{Binding Thumbnail, Converter={StaticResource ByteArrayToImageConverter}}" 
                                       HorizontalAlignment="Center" />
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding OriginalFileName}" Foreground="Gray" FontSize="12" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Pagination -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="‹ Önceki" Style="{StaticResource ModernButton}" Width="100"
                    Command="{Binding PreviousPageCommand}" IsEnabled="{Binding CanGoPrevious}" />
            <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="14" Margin="20,0" />
            <Button Content="Sonraki ›" Style="{StaticResource ModernButton}" Width="100"
                    Command="{Binding NextPageCommand}" IsEnabled="{Binding CanGoNext}" />
        </StackPanel>
    </Grid>
</Window>
